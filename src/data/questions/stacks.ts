import { Question } from "./sorting";

export const stacksQuestions: Question[] = [
  { id: "st-1", type: "tf", text: "In a stack, both insertion (push) and deletion (pop) occur only at the top end, giving Last-In-First-Out (LIFO) behaviour.", marks: 1, answer: true, explanation: "A stack restricts all insertion and deletion to one end called the top, which is exactly the LIFO discipline." },
  { id: "st-2", type: "tf", text: "The top() / peek() operation must scan from the bottom of the array to find the topmost element, making it O(n).", marks: 1, answer: false, explanation: "The top index is tracked directly, so top() reads data[top] in a single step — it is O(1) and performs no scan." },
  { id: "st-3", type: "tf", text: "When two stacks share one array by growing from opposite ends, overflow happens only when their two tops meet, i.e. top1 + 1 == top2.", marks: 1, answer: true, explanation: "With shared memory the collision condition top1+1==top2 is the only overflow point; until then either stack can keep growing." },
  { id: "st-4", type: "tf", text: "Overflow on a push is an inherent limitation of the Stack ADT itself and cannot be avoided by any implementation.", marks: 1, answer: false, explanation: "Overflow is a limitation of the array-based implementation only; a linked-list stack pushes at the head and grows freely." },
  { id: "st-5", type: "tf", text: "Because the exponent operator ^ is right-associative, in infix-to-postfix conversion ICP(^) is set greater than ISP(^), so a new ^ is pushed on top of an existing ^ rather than popping it.", marks: 1, answer: true, explanation: "Right-associativity is encoded by ICP(^) > ISP(^), which prevents popping the earlier ^ so a^b^c becomes a b c ^ ^." },
  { id: "st-6", type: "mcq", text: "An empty stack is popped using a 0-based top index where empty is represented by top == -1. What error condition does this attempted pop trigger?", marks: 2, options: ["Overflow", "Underflow", "Collision", "Out-of-order access"], answer: "b", explanation: "Removing from an already-empty stack is underflow; overflow is the opposite case of pushing onto a full stack." },
  { id: "st-7", type: "mcq", text: "A stack of capacity n stores its item count in a variable top (top counts the elements present). Which test correctly detects that the stack is FULL before a push?", marks: 2, options: ["top == -1", "top == 0", "top == n", "top == n + 1"], answer: "c", explanation: "When top counts items, all n slots are used when top == n, so that is the overflow test for this convention." },
  { id: "st-8", type: "mcq", text: "Which application is the WORST fit for a stack's LIFO behaviour?", marks: 2, options: ["Undo history in a text editor", "Tracking active function calls at run time", "Serving print jobs in the exact order they were submitted", "Matching nested brackets while parsing"], answer: "c", explanation: "Serving jobs in submission order is First-In-First-Out (a queue); the other three are classic LIFO stack uses." },
  { id: "st-9", type: "mcq", text: "Three or more stacks are packed into a single array. When one stack overflows into a neighbour's region, what must happen, and at what typical cost?", marks: 2, options: ["Neighbouring stacks (and their boundaries) are shifted to make room, costing O(n)", "Nothing — opposite-end growth prevents any collision, O(1)", "The overflowing stack is silently truncated, O(1)", "All stacks are merged into one, O(log n)"], answer: "a", explanation: "With 3+ stacks the opposite-ends trick no longer applies, so neighbours must be shifted to free space — an O(n) reorganisation." },
  { id: "st-10", type: "mcq", text: "Using stack-based delimiter matching, which of these strings is UNBALANCED?", marks: 2, options: ["( [ ] { } )", "{ [ ( ) ] }", "( ( ) ) [ ]", "[ ( ] )"], answer: "d", explanation: "In [ ( ] ) the closer ] is met while ( is on top, so the popped opener does not match — the string is unbalanced." },
  { id: "st-11", type: "mcq", text: "While converting infix to postfix, the incoming operator is '+' and the operator currently on top of the stack is '*'. What does the algorithm do first?", marks: 2, options: ["Push '+' immediately on top of '*'", "Pop '*' to the output, then continue comparing", "Discard '+' entirely", "Pop both '*' and the next operand"], answer: "b", explanation: "Since ISP(*) >= ICP(+), the higher-precedence '*' is popped to output before '+' is eventually pushed." },
  { id: "st-12", type: "fill", text: "The error that occurs when push() is called on a stack whose array is already full is called ______.", marks: 1, answer: "overflow", acceptedAnswers: ["overflow", "Overflow", "stack overflow", "StackFull", "stack full"], explanation: "Pushing onto a full array-based stack is the overflow (StackFull) condition." },
  { id: "st-13", type: "fill", text: "Converting the infix expression a * b ^ c (where ^ is exponent) to postfix gives the output ______.", marks: 1, answer: "a b c ^ *", acceptedAnswers: ["a b c ^ *", "abc^*", "a b c ^ * ", "a b c^*"], explanation: "^ has higher precedence than *, so b^c is formed first (b c ^), then multiplied by a, giving a b c ^ *." },
  { id: "st-14", type: "trace", text: "Trace the infix-to-postfix conversion of ( m + n ) * k using an operator stack and give the final postfix expression.", marks: 2, answer: "m n + k *", acceptedAnswers: ["m n + k *", "mn+k*", "m n + k * ", "m n +k*"], explanation: "'(' is pushed; m and n are output; '+' pushed; ')' pops '+' to output (m n +) and discards the parentheses; '*' pushed; k output; end pops '*' giving m n + k *." },
  // ── Additional practice ──
  { id: "stx-1", type: "tf", text: "In the linked-list implementation of a stack described in the lecture, both push and pop operate on the head (front) of the list, so each runs in O(1) time without ever traversing the list.", marks: 1, answer: true, explanation: "The slides state the top of the stack is the head node: push adds to the front (P->next = S; S = P) and pop removes from the front (S = S->next), both O(1) with no traversal." },
  { id: "stx-2", type: "tf", text: "When a recursive function executes, the run-time system uses a queue (FIFO) to keep track of the chain of pending method calls.", marks: 1, answer: false, explanation: "The lecture lists the chain of method calls in the run-time system as a stack (LIFO) application; the most recently called method is the first to return." },
  { id: "stx-3", type: "tf", text: "The classic stack-processing pattern in the lecture (pop everything from stack s into a temporary stack t, then pop t back into s) leaves the original stack s in its original order because it reverses the elements twice.", marks: 1, answer: true, explanation: "Moving s into t reverses the order once, and moving t back into s reverses it a second time, so s is restored to its original top-to-bottom order after the two-pass traversal." },
  { id: "stx-4", type: "mcq", text: "A stack is initially empty. The following operations are performed in order: Push(A), Push(B), Pop, Push(C), Push(D), Pop, Push(E). Reading from BOTTOM to TOP, what does the stack contain afterward?", marks: 2, options: ["A, C, E", "A, B, C, E", "E, C, A", "A, C, D, E"], answer: "a", explanation: "After Push A,B then Pop removes B leaving [A]; Push C,D then Pop removes D leaving [A,C]; Push E gives bottom-to-top A, C, E." },
  { id: "stx-5", type: "mcq", text: "Using the lecture's right-to-left method, what is the value of the prefix expression  + * 3 4 - 9 2 ?", marks: 2, options: ["19", "14", "17", "21"], answer: "a", explanation: "Scanning right to left and pushing operands: '- 9 2' = 9 - 2 = 7, '* 3 4' = 12, then '+' gives 12 + 7 = 19." },
  { id: "stx-6", type: "mcq", text: "Which sequence of operations correctly distinguishes the auxiliary operation top() (peek) from pop() as defined in the lecture?", marks: 2, options: ["top() returns the last inserted element WITHOUT removing it, while pop() removes it", "top() removes and returns the bottom element, while pop() removes the top", "top() and pop() both remove the top element but pop() also returns it", "top() returns the number of elements, while pop() returns the top element"], answer: "a", explanation: "The slides define top() as returning the last inserted element without removing it, whereas pop() removes (and returns) the last inserted element; size() is the operation that returns the count." },
  { id: "stx-7", type: "fill", text: "Evaluate the postfix expression  6 2 / 5 +  using a stack. The final result left on the stack is ______.", marks: 1, answer: "8", acceptedAnswers: ["8", "eight"], explanation: "Push 6 and 2; '/' pops them giving 6/2 = 3; push 5; '+' pops 3 and 5 giving 3 + 5 = 8." },
  { id: "stx-8", type: "trace", text: "Trace the infix-to-postfix conversion of  a * ( b + c )  using an operator stack (scan left to right; on ')' pop until '(' is removed). Give the final postfix expression.", marks: 2, answer: "a b c + *", acceptedAnswers: ["a b c + *", "abc+*", "a b c+*", "abc + *"], explanation: "Output a; push *; push (; output b; push +; output c; on ')' pop + (output) and discard parentheses; at end pop * giving a b c + *." },
];
